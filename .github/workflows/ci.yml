name: CI
on: [pull_request, push, workflow_dispatch]
permissions: { contents: read }
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
 chore/uplift-v4-20260828-053524
      - uses: actions/checkout@v4

      - uses: actions/checkout@v7
 master
      - run: echo "Add project tests here"
